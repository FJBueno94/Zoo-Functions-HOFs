// const { species } = require('../data/zoo_data');

function countAnimals(animal) {
//   const animal = species.find((specie) => specie.name);
//   if (animal === {}) {

//   };
//   if (animal === {species: animal.name})
}

module.exports = countAnimals;
